{% extends 'admin/base.html.twig' %}

{% block title %}Détail Filière{% endblock %}

{% block body %}
<div class="page-header">
    <div class="page-block">
        <div class="row align-items-center">
            <div class="col-md-8">
                <div class="page-header-title">
                    <h5 class="m-b-10">Détail Filière</h5>
                    <p class="m-b-0">Consultation des informations</p>
                </div>
            </div>
            <div class="col-md-4">
                <ul class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('app_admin_dashboard') }}"><i class="fa fa-home"></i></a></li>
                    <li class="breadcrumb-item"><a href="{{ path('app_admin_filiere_index') }}">Filières</a></li>
                    <li class="breadcrumb-item"><a href="#">Détail</a></li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="pcoded-inner-content">
    <div class="main-body">
        <div class="page-wrapper">
            <div class="page-body">
                <div class="card">
                    <div class="card-header">
                        <h5>{{ filiere.nom }}</h5>
                        <div class="card-header-right">
                            <a href="{{ path('app_admin_filiere_edit', {'id': filiere.id}) }}" class="btn btn-warning btn-sm">✏️ Modifier</a>
                            <a href="{{ path('app_admin_filiere_index') }}" class="btn btn-secondary btn-sm">↩️ Retour</a>
                        </div>
                    </div>
                    <div class="card-block">
                        <table class="table table-bordered">
                            <tbody>
                                <tr>
                                    <th style="width: 200px;">Id</th>
                                    <td>{{ filiere.id }}</td>
                                </tr>
                                <tr>
                                    <th>Nom</th>
                                    <td>{{ filiere.nom }}</td>
                                </tr>
                                <tr>
                                    <th>Description</th>
                                    <td>{{ filiere.description }}</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer">
                        {{ include('admin/filiere/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}